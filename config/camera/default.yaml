# @package _global_.camera
# 默认摄像头配置

# 摄像头索引
index: 0

# 2K分辨率设置
width: 1920
height: 1080

# 抽帧配置
extract_interval: 1  # 每1秒抽取一帧

# 视频编码
codec: mp4v
fps: 30.0

# 输出路径
output:
  video_dir: data/outputs/videos
  frame_dir: data/outputs/frames
