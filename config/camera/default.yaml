# @package _global_.camera
# 默认摄像头配置

# 摄像头索引（0=主摄像头, 2=备用摄像头）
index: 0

# 2K分辨率设置
width: 1920
height: 1080

# 抽帧配置
extract_interval: 0.5  # 每0.5秒抽取一帧

# 视频编码
codec: mp4v
fps: 30.0

# 输出路径
output:
  video_dir: data/outputs/videos
  frame_dir: data/outputs/frames
